import 'package:flutter/material.dart';

/// Master cabinet accent — dark navy (not client green).
const masterNavy = Color(0xFF1D243D);
const masterNavyLight = Color(0xFF2A3555);
const masterPageBg = Color(0xFFF4F6FA);
